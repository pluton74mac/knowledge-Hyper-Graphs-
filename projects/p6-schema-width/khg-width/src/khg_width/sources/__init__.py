"""Schema generators for the survey (DESIGN §6.2): ``wikidata`` (wd-roles r1 and the relation-local control),
``biolink`` (linkml imported lazily) and ``manifest`` (sha256 verification before generation)."""
